<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.board5.mapper.BoardMapper">

	<insert id="boardInsert" parameterType="com.example.board5.dto.BoardDTO">
	  insert into
	  board7 (bno, subject, content, writer, reg_date)
	  values(0,#{subject},#{content},#{writer},NOW())
	</insert>

</mapper>